@import "tailwindcss";

@theme inline {
  --color-primary: #059669;
  --color-primary-light: #34D399;
  --color-primary-dark: #047857;
  --color-secondary: #10B981;
  --color-accent: #F59E0B;
  --color-accent-dark: #D97706;
  --color-surface: #ffffff;
  --color-surface-alt: #f8fafc;
  --color-surface-dark: #0f172a;
  --color-surface-dark-alt: #1e293b;
  --color-text: #111827;
  --color-text-light: #111827;
  --color-text-dark: #F1F5F9;
  --color-text-dark-muted: #94a3b8;
  --color-border: #D1D5DB;
  --color-border-dark: #334155;
  --color-page-bg: #F3F4F6;
  --color-page-bg-dark: #0f172a;
  --color-muted: #374151;
  --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
}

@layer base {
  body {
    @apply bg-page-bg text-text font-sans antialiased;
  }
  .dark body,
  .dark {
    @apply bg-page-bg-dark text-text-dark;
  }
}

@layer components {
  .card {
    @apply bg-surface border border-border rounded-xl p-6;
    box-shadow: 0 2px 8px 0 rgba(0, 0, 0, 0.08);
  }
  .dark .card {
    @apply bg-surface-dark-alt border-border-dark;
    box-shadow: 0 2px 8px 0 rgba(0, 0, 0, 0.3);
  }
  .btn-primary {
    @apply bg-primary text-surface px-6 py-3 rounded-lg font-semibold transition-all duration-200 hover:bg-primary-dark cursor-pointer;
  }
  .btn-secondary {
    @apply bg-surface border border-border text-text px-6 py-3 rounded-lg font-semibold transition-all duration-200 hover:bg-surface-alt cursor-pointer;
  }
  .dark .btn-secondary {
    @apply bg-surface-dark-alt border-border-dark text-text-dark hover:bg-surface-dark;
  }
  .input-field {
    @apply w-full px-4 py-3 text-lg border border-border rounded-lg bg-surface text-text focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary transition-all duration-200;
  }
  .dark .input-field {
    @apply bg-surface-dark border-border-dark text-text-dark;
  }
  .select-field {
    @apply w-full px-4 py-3 text-lg border border-border rounded-lg bg-surface text-text focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary transition-all duration-200 cursor-pointer;
  }
  .dark .select-field {
    @apply bg-surface-dark border-border-dark text-text-dark;
  }
}
